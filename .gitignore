/target
/data
*.mmdb
stomatopod.toml
!tests/e2e/stomatopod.toml
.env
*.log
node_modules/
assets/dist/
# Built Dioxus SPA bundle (produced by `mise run ui:bundle`); rebuilt in CI /
# Docker. The directory itself is kept via .gitkeep so rust-embed's
# `#[folder = "ui-dist/"]` always resolves.
/crates/web/ui-dist/*
!/crates/web/ui-dist/.gitkeep
.DS_Store
